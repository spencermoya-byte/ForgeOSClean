# Marketplace Scripts

This directory contains utility scripts for marketplace operations.

## Scripts

- `install-extension.sh` - Script to install extensions
- `update-extension.sh` - Script to update extensions
- `check-compatibility.sh` - Script to check extension compatibility
- `generate-metadata.sh` - Script to generate extension metadata
