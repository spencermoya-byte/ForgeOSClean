import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return <h1>Hello, Tauri with React and TypeScript!</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));
